# Model publication appearances explicitly

Newspaper issues and works are connected through persisted many-to-many publication appearances rather than copied text fields. This supports navigation in both directions and allows paper searches to match the title, author, or description of included works, while retaining optional page or section context for each appearance.
